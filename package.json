{
  "type": "module",
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && tsup"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.12",
    "@types/node": "^24.5.2",
    "chrome-types": "^0.1.379",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.75.1"
  }
}
